type Query {
  """
  Get articles
  """
  articles(articlesInput: ArticlesInput!): ArticleConnection!
}

input ArticlesInput {
  userId: String
  isEng: Boolean
  tag: String
  first: Int
  after: String
  last: Int
  before: String
}
