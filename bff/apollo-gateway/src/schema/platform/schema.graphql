"""
Platform schema
"""
type Platform implements Node {
  id: ID!
  name: String!
  siteUrl: String!
  platformSiteType: Int!
  faviconUrl: String!
  isEng: Boolean!
  createdAt: Int!
  updatedAt: Int!
  deletedAt: Int
}
